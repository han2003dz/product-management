mixin alert-success(time)
  - if (messages.success)
    .message.info
        .alert.alert-success(
          show-alert 
          data-time=time
        ) #{messages.success} <span close-alert>x</span>